{% extends 'base.html.twig' %}

{% block title %}Exercise{% endblock %}

{% block body %}
    <h1>Exercise</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exercise.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ exercise.name }}</td>
            </tr>
            <tr>
                <th>MuscleGroup</th>
                <td>{{ exercise.muscleGroup }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_exercise_index') }}">back to list</a>

    <a href="{{ path('app_exercise_edit', {'id': exercise.id}) }}">edit</a>

    {{ include('exercise/_delete_form.html.twig') }}
{% endblock %}
